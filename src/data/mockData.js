// Minimal fallback data structure for components that still import mockData.
// Does NOT contain any real demo data; everything is empty and UI relies on real API.

export const mockData = {
  faculties: [],
  departments: [],
  teachers: [],
  reviews: [],
}

